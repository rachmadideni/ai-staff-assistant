{
  "title": "AI Staff Assistant MVP",
  "source": "JOB.md",
  "stories": [
    {
      "id": 1,
      "milestone": 2,
      "title": "Supabase project setup",
      "description": "Initialize Supabase project, configure auth settings, set up environment variables, enable pgvector extension.",
      "checks": [
        "Supabase project exists and is linked",
        "Auth settings configured (email/password signup enabled)",
        "pgvector extension enabled in database",
        "NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY set in .env.local",
        "OPENAI_API_KEY set in .env.local"
      ],
      "passes": true
    },
    {
      "id": 2,
      "milestone": 2,
      "title": "Tenants table migration + CRUD API",
      "description": "Create tenants table with all required fields (name, slug, settings, active status, usage limits, created_at, updated_at). Implement CRUD API routes. Enable RLS.",
      "checks": [
        "tenants table exists with all required columns",
        "RLS enabled on tenants table",
        "Super admin can create, list, get, update, deactivate tenants",
        "Client admin cannot access other tenants' data",
        "API returns 403 for unauthorized cross-tenant access"
      ],
      "passes": true
    },
    {
      "id": 3,
      "milestone": 2,
      "title": "Users table + auth trigger",
      "description": "Create public.users table referencing auth.users.id. Add role enum (super_admin, client_admin). Create database trigger to auto-create public.users row on auth.users signup. Create invitation/registration flow for client admins.",
      "checks": [
        "public.users table exists with id, email, role, tenant_id, created_at",
        "Auth trigger auto-creates public.users row on signup",
        "Super admin can invite client admin users",
        "Client admin users created with correct tenant_id"
      ],
      "passes": true
    },
    {
      "id": 4,
      "milestone": 2,
      "title": "Super Admin login page",
      "description": "Create login page with Supabase Auth (email/password). On login, verify role is super_admin. Redirect to super admin dashboard. Handle errors, password reset.",
      "checks": [
        "Login form accepts email and password",
        "Super admin credentials redirect to /dashboard/super-admin",
        "Client admin credentials redirect to /dashboard/client-admin",
        "Invalid credentials show error message",
        "Password reset flow works"
      ],
      "passes": true
    },
    {
      "id": 5,
      "milestone": 2,
      "title": "Client Admin login page",
      "description": "Create login page scoped to client admin users. On login, verify role is client_admin and user is linked to a tenant. Redirect to client admin dashboard.",
      "checks": [
        "Client admin login works",
        "Login restricted to client_admin role only",
        "User sees only their own tenant's data after login",
        "Super admin cannot log in here (redirect to super admin login)"
      ],
      "passes": true
    },
    {
      "id": 7,
      "milestone": 3,
      "title": "business_access_tokens table + API",
      "description": "Create business_access_tokens table (tenant_id, token, pin_code, label, active, expires_at, created_at). Implement generate/reset/revoke API. Tokens are UUIDs. PIN is optional numeric code.",
      "checks": [
        "Table exists with all required columns",
        "Generate token API creates unique UUID token",
        "Token resolves to correct tenant",
        "Reset token generates new UUID, invalidates old one",
        "Revoke token sets active=false",
        "Expired token returns 403"
      ],
      "passes": true
    },
    {
      "id": 8,
      "milestone": 3,
      "title": "Staff chat page (token-based access)",
      "description": "Create /staff/[token] page. On load, validate token, identify business. Show chat interface with business name. No login required. Session tracks business context. Handle invalid/expired tokens.",
      "checks": [
        "Valid token shows chat page with correct business name",
        "Invalid token shows error/redirect",
        "Expired/revoked token shows access denied",
        "No login required to access chat",
        "Business context correctly identified from token"
      ],
      "passes": true
    },
    {
      "id": 9,
      "milestone": 3,
      "title": "Optional PIN screen",
      "description": "If business_access_token has pin_code set, show PIN entry screen before chat. Validate PIN client-side and server-side. Wrong PIN denies access. Correct PIN enters chat. Rate-limit PIN attempts.",
      "checks": [
        "Token with pin_code shows PIN screen before chat",
        "Correct PIN enters chat",
        "Wrong PIN shows error (max 5 attempts before lockout)",
        "Token without pin_code goes directly to chat",
        "PIN validation happens server-side, not just client-side"
      ],
      "passes": true
    },
    {
      "id": 10,
      "milestone": 3,
      "title": "QR code generation in admin dashboard",
      "description": "Add QR code display to both super admin and client admin dashboards. QR code encodes the staff access URL. Generate QR code using qrcode.react component.",
      "checks": [
        "Super admin can view QR code for any business",
        "Client admin can view QR code for their own business",
        "QR code scans to correct /staff/[token] URL",
        "QR code regenerates when token is reset"
      ],
      "passes": true
    },
    {
      "id": 11,
      "milestone": 3,
      "title": "Access link disable/reset admin UI",
      "description": "Add admin UI to disable, enable, and reset staff access tokens. Show token status in admin dashboard. Confirm dialog for destructive actions.",
      "checks": [
        "Super admin can disable/enable/reset any business's token",
        "Client admin can disable/enable/reset their own business's token",
        "Disabled token immediately stops working",
        "UI shows confirmation dialog before destructive actions"
      ],
      "passes": true
    },
    {
      "id": 12,
      "milestone": 4,
      "title": "Document upload API + Storage",
      "description": "Create document upload endpoint. Store files in Supabase Storage under tenant_id/ prefix. Accept PDF, DOCX, TXT, MD. Validate file type and size (max 10MB). Create documents table with status workflow.",
      "checks": [
        "File uploads stored in Supabase Storage under correct tenant prefix",
        "Accepted file types: PDF, DOCX, TXT, MD",
        "Rejected file types return 400",
        "Files over 10MB rejected",
        "Document record created in documents table with status='draft'"
      ],
      "passes": true
    },
    {
      "id": 13,
      "milestone": 4,
      "title": "Document status workflow",
      "description": "Implement document status transitions: draft -> approved -> archived. Only approved documents available for AI retrieval. Admin UI to approve, archive, or delete documents. Status change updates vector index accordingly.",
      "checks": [
        "Document can transition from draft to approved",
        "Document can transition from approved to archived",
        "Document can be deleted (soft or hard)",
        "Only approved documents appear in vector search results",
        "Status change triggers re-indexing"
      ],
      "passes": true
    },
    {
      "id": 14,
      "milestone": 4,
      "title": "OpenAI embeddings + pgvector indexing",
      "description": "Generate embeddings using OpenAI text-embedding-3-small. Store in Supabase pgvector. Index by tenant_id for isolated retrieval. Implement vector search function filtered by tenant_id.",
      "checks": [
        "Embeddings generated via OpenAI API (not client-side)",
        "Vectors stored in pgvector with tenant_id column",
        "Vector search returns only documents for the requesting tenant",
        "Search returns top 10 most relevant chunks",
        "Embedding dimension matches text-embedding-3-small (1536)"
      ],
      "passes": true
    },
    {
      "id": 15,
      "milestone": 4,
      "title": "Document replacement + re-indexing",
      "description": "When document is replaced or updated, delete old vectors, re-embed new content, re-insert. Handle race conditions during re-indexing. Show indexing status in document list.",
      "checks": [
        "Replaced document gets new embeddings (old vectors deleted)",
        "Archived document vectors removed from search index",
        "Index status shows processing/active/failed",
        "Concurrent indexing requests handled gracefully"
      ],
      "passes": true
    },
    {
      "id": 16,
      "milestone": 5,
      "title": "OpenAI chat completion endpoint",
      "description": "Backend-only API route for OpenAI chat completions. Accept messages + tenant context. Call OpenAI API server-side. Return streaming or complete response. NEVER expose API key to client.",
      "checks": [
        "API route at /api/chat accepts POST with messages and tenant_id",
        "OpenAI API key NOT visible in browser network tab",
        "Response contains answer text",
        "Errors handled gracefully (no stack traces exposed)",
        "Model configurable via environment variable"
      ],
      "passes": true
    },
    {
      "id": 17,
      "milestone": 5,
      "title": "Business-scoped pgvector retrieval",
      "description": "Before LLM call, retrieve relevant chunks from pgvector filtered by tenant_id. Include retrieved context in the LLM prompt. Chunks must belong to the requesting business only.",
      "checks": [
        "Retrieval queries filter by tenant_id",
        "Only approved documents are included in retrieval",
        "Top 5-10 chunks passed to LLM as context",
        "Business A token never retrieves Business B chunks",
        "Retrieval timeout handled gracefully"
      ],
      "passes": true
    },
    {
      "id": 18,
      "milestone": 5,
      "title": "Strict system prompt with escalation rules",
      "description": "Create system prompt enforcing: answer only from approved docs, no inventing, escalate for sensitive topics. Escalation categories: injuries, emergencies, harassment, discrimination, payroll, termination, legal, medical, refunds outside policy.",
      "checks": [
        "System prompt loaded from server-side config",
        "Prompt instructs to answer only from provided context",
        "Prompt includes escalation categories",
        "Escalation response: 'I can't find an approved answer. Please ask your manager.'",
        "Urgent escalation: 'This may require immediate manager involvement.'"
      ],
      "passes": true
    },
    {
      "id": 19,
      "milestone": 5,
      "title": "Source citation in answers",
      "description": "Include document name/source reference in AI answers. Format: 'According to [Document Name]...'. Show which documents were used for each answer in conversation log.",
      "checks": [
        "Answer includes document reference when possible",
        "Source documents logged in conversation record",
        "Answer without source if no matching document found (escalation)",
        "Citation format: 'According to [document name] - [section]'"
      ],
      "passes": true
    },
    {
      "id": 20,
      "milestone": 5,
      "title": "Mobile-responsive chat UI",
      "description": "Build chat interface with: large readable text, simple input area, disclaimer banner, escalation guidance, business name header. Mobile-first responsive design. Fast initial load.",
      "checks": [
        "Chat page works on mobile viewport (375px width)",
        "Text is readable without zooming",
        "Disclaimer visible near input area",
        "Business name displayed in header",
        "Page loads in under 3 seconds on mobile connection",
        "Lighthouse mobile score > 80"
      ],
      "passes": true
    },
    {
      "id": 21,
      "milestone": 6,
      "title": "Super Admin dashboard",
      "description": "Full super admin dashboard with: list all businesses, create business, edit business settings, disable business, view all usage, view all conversation logs, manage client admin users per business.",
      "checks": [
        "Super admin sees all businesses in table/list",
        "Create business form works (creates tenant + access token)",
        "Edit business settings works",
        "Disable business immediately stops staff access",
        "Super admin can view logs for any business"
      ],
      "passes": true
    },
    {
      "id": 22,
      "milestone": 6,
      "title": "Client Admin dashboard",
      "description": "Client admin dashboard with: business settings view, staff access link + QR code display, document list, reset access code, view own conversation logs, view own usage.",
      "checks": [
        "Client admin sees only their own business data",
        "Staff access link and QR code displayed",
        "Document list shows only own business documents",
        "Cannot access other businesses' data via URL manipulation",
        "Usage stats shown for current month"
      ],
      "passes": true
    },
    {
      "id": 23,
      "milestone": 6,
      "title": "Conversation logs table + view",
      "description": "Store staff questions, AI answers, timestamps, source documents, escalation flag, token usage. Per-business view for client admin. Global view for super admin. Search/filter by date.",
      "checks": [
        "Each conversation log has: question, answer, timestamp, sources, escalation_flag, token_count",
        "Client admin sees only their business's logs",
        "Super admin sees all logs with tenant filter",
        "Logs filterable by date range",
        "Escalated questions flagged and filterable"
      ],
      "passes": true
    },
    {
      "id": 24,
      "milestone": 6,
      "title": "Usage tracking per business",
      "description": "Track monthly: question count, AI response count, escalation count, token usage, estimated cost. Monthly reset. Display in both dashboards. Store usage in usage_tracking table.",
      "checks": [
        "Usage counter increments with each staff question",
        "Usage data scoped by tenant_id and month",
        "Client admin sees their own usage",
        "Super admin sees all businesses' usage",
        "Monthly reset works (counters start fresh each month)"
      ],
      "passes": true
    },
    {
      "id": 25,
      "milestone": 6,
      "title": "Usage caps + near-limit warning",
      "description": "Per-business monthly question limit (configurable by super admin). Warning banner when approaching limit (80%). Block questions when limit reached. Super admin can adjust limits per business.",
      "checks": [
        "Default limit: 500 questions/month",
        "Warning banner shown at 80% usage",
        "Questions blocked when limit reached with clear message",
        "Super admin can adjust limit per business",
        "Limit check happens server-side (not just client-side)"
      ],
      "passes": true
    },
    {
      "id": 26,
      "milestone": 6,
      "title": "Escalated/missing questions report",
      "description": "Report page showing questions where assistant said 'ask a manager'. Helpful for identifying gaps in knowledge base. Both admin roles can view (scoped).",
      "checks": [
        "Shows questions that received escalation response",
        "Client admin sees only their business's escalations",
        "Super admin sees all escalations with tenant filter",
        "Identifies patterns in missing knowledge",
        "Exportable or shareable view"
      ],
      "passes": true
    },
    {
      "id": 27,
      "milestone": 7,
      "title": "50 test cases execution",
      "description": "Run 50 realistic test cases covering: questions answered in docs, questions not covered, sensitive topics requiring escalation, cross-tenant isolation, rate limiting, mobile chat, document re-indexing.",
      "checks": [
        "15+ questions clearly answered in documents pass",
        "10+ questions not covered return escalation",
        "10+ sensitive topics return escalation",
        "5 cross-tenant isolation tests pass (Token A cannot access Business B)",
        "5 rate-limiting/abuse tests pass",
        "5 mobile/responsive tests pass"
      ],
      "passes": true
    },
    {
      "id": 28,
      "milestone": 7,
      "title": "Deploy to Vercel",
      "description": "Deploy application to Vercel. Configure environment variables. Set up production Supabase project. Verify all features work in production. Configure custom domain if available.",
      "checks": [
        "App accessible at production URL",
        "All environment variables configured in Vercel",
        "Supabase production project linked",
        "Auth redirect URLs configured for production",
        "All API routes work in production",
        "HTTPS enforced"
      ],
      "passes": true
    },
    {
      "id": 29,
      "milestone": 7,
      "title": "Admin handover documentation",
      "description": "Create handover docs: setup instructions, how to add a new business, how to add client admins, how to generate/reset access links, how to upload/re-index documents, how to view usage/logs, troubleshooting guide.",
      "checks": [
        "Setup instructions complete (env vars, Supabase, deploy)",
        "Step-by-step guide for adding a new business",
        "How to add/remove client admin users",
        "How to generate and reset staff access links",
        "How to upload, approve, archive documents",
        "Troubleshooting section for common issues"
      ],
      "passes": true
    },
    {
      "id": 30,
      "milestone": 6,
      "title": "Document upload UI for Client Admin",
      "description": "Build upload form and document management UI in the Client Admin dashboard. Users can upload PDF, DOCX, TXT, MD files. Show document list with status (draft/approved/archived). Allow approve/archive/delete actions. Integrate with existing POST /api/documents and PATCH /api/documents/[id] endpoints.",
      "checks": [
        "Upload form accepts file and calls POST /api/documents",
        "File type validation shows error for unsupported types",
        "File size validation (max 10MB) shows error for oversized files",
        "Upload progress shown during upload",
        "Document list shows filename, status, and upload date",
        "Client admin can approve, archive, delete their own documents",
        "Super admin can manage documents for any business",
        "Status changes reflected immediately in the list"
      ],
      "passes": true
    },
    {
      "id": 31,
      "milestone": 8,
      "title": "Fix deprecated auth.role() in storage policies",
      "description": "Replace deprecated auth.role() = 'authenticated' with TO authenticated clause in storage RLS policies. The auth.role() function is deprecated and breaks silently when anonymous sign-ins are enabled.",
      "checks": [
        "Storage INSERT policy uses TO authenticated instead of auth.role()",
        "Storage SELECT policy uses TO authenticated instead of auth.role()",
        "Storage DELETE policy uses TO authenticated instead of auth.role()",
        "All storage policies still enforce tenant scoping for client_admin",
        "Typecheck passes after migration"
      ],
      "passes": true
    },
    {
      "id": 32,
      "milestone": 8,
      "title": "Separate HMAC secret from service role key",
      "description": "Use a dedicated HMAC_SECRET env var instead of SUPABASE_SERVICE_ROLE_KEY for HMAC signing. Remove authPayload from URL query strings (documents GET route) to prevent token leakage in server logs.",
      "checks": [
        "HMAC_SECRET env var added to .env.local.example",
        "auth-utils.ts uses HMAC_SECRET instead of SUPABASE_SERVICE_ROLE_KEY",
        "invite/route.ts uses HMAC_SECRET instead of SUPABASE_SERVICE_ROLE_KEY",
        "documents GET route accepts authPayload in request body or header, not query string",
        "Typecheck passes"
      ],
      "passes": true
    },
    {
      "id": 33,
      "milestone": 8,
      "title": "Restrict SECURITY DEFINER function access",
      "description": "Revoke PUBLIC EXECUTE from verify_access_pin to prevent brute-force PIN attacks. Grant EXECUTE only to specific roles (anon). validate_access_token and match_document_chunks should also have restricted access.",
      "checks": [
        "REVOKE EXECUTE ON FUNCTION verify_access_pin FROM PUBLIC",
        "GRANT EXECUTE ON FUNCTION verify_access_pin TO anon",
        "REVOKE EXECUTE ON FUNCTION validate_access_token FROM PUBLIC",
        "GRANT EXECUTE ON FUNCTION validate_access_token TO anon",
        "Functions still work via Supabase RPC calls",
        "Typecheck passes"
      ],
      "passes": true
    },
    {
      "id": 34,
      "milestone": 8,
      "title": "Fix storage SELECT policy tenant scoping",
      "description": "The storage SELECT policy currently allows any authenticated user to read all files in the documents bucket. Add tenant folder scoping so client_admin can only read their own tenant's files.",
      "checks": [
        "Storage SELECT policy scopes client_admin to own tenant folder",
        "Super admin can still read all files",
        "Client admin cannot read other tenants' files via URL manipulation",
        "Typecheck passes"
      ],
      "passes": true
    },
    {
      "id": 35,
      "milestone": 8,
      "title": "Fix usage tracking token_count accumulation",
      "description": "The chat API upserts usage_tracking with token_count set to the latest completion's count instead of accumulating. Fix to add new tokens to existing count.",
      "checks": [
        "token_count accumulates across questions in the same month",
        "question_count still increments correctly",
        "escalation_count still increments correctly",
        "Typecheck passes"
      ],
      "passes": true
    },
    {
      "id": 36,
      "milestone": 8,
      "title": "Add missing indexes and optimize vector search",
      "description": "Add created_at indexes on conversations and audit_logs for date-range queries. Consider HNSW index instead of IVFFlat for better vector search performance. Add PIN hash index for verify_access_pin lookups.",
      "checks": [
        "created_at index on conversations table",
        "created_at index on audit_logs table",
        "IVFFlat lists parameter reviewed for data size",
        "Typecheck passes"
      ],
      "passes": true
    },
    {
      "id": 37,
      "milestone": 9,
      "title": "Fix HMAC key mismatch in dashboard pages",
      "description": "Dashboard Server Components (super-admin/page.tsx, client-admin/page.tsx) still use SUPABASE_SERVICE_ROLE_KEY to sign HMAC payloads, but auth-utils.ts now uses HMAC_SECRET. This breaks invite and document operations in production.",
      "checks": [
        "super-admin/page.tsx uses HMAC_SECRET for signing",
        "client-admin/page.tsx uses HMAC_SECRET for signing",
        "Typecheck passes"
      ],
      "passes": true
    },
    {
      "id": 38,
      "milestone": 9,
      "title": "Add error boundaries and loading states",
      "description": "Add error.tsx and loading.tsx files to key routes (dashboard, staff chat, auth pages). Users currently see generic Next.js error pages on failures.",
      "checks": [
        "error.tsx exists for dashboard routes",
        "loading.tsx exists for dashboard routes",
        "error.tsx exists for staff chat page",
        "Typecheck passes"
      ],
      "passes": true
    },
    {
      "id": 39,
      "milestone": 9,
      "title": "Remove force-dynamic from API routes",
      "description": "All API routes set force-dynamic which defeats Next.js caching. Only routes that read cookies/headers need this. Remove from routes that don't need it.",
      "checks": [
        "API routes that only read request body don't set force-dynamic",
        "Routes that read cookies/headers keep force-dynamic",
        "Typecheck passes"
      ],
      "passes": true
    },
    {
      "id": 40,
      "milestone": 9,
      "title": "Add dynamic metadata for pages",
      "description": "Staff chat page and dashboard pages should set dynamic metadata (title, description) using generateMetadata. Currently all pages show generic 'AI Staff Assistant'.",
      "checks": [
        "Staff chat page shows business name in title",
        "Dashboard pages show role-appropriate titles",
        "Typecheck passes"
      ],
      "passes": true
    },
    {
      "id": 41,
      "milestone": 9,
      "title": "Replace select(*) with specific columns",
      "description": "Multiple queries use select(*) fetching all columns when only a few are needed. This wastes bandwidth and increases payload size.",
      "checks": [
        "client-admin/page.tsx selects specific columns for documents, tokens, usage",
        "super-admin/page.tsx selects specific columns for tenants, usage, tokens",
        "conversation-logs.tsx selects specific columns",
        "Typecheck passes"
      ],
      "passes": true
    },
    {
      "id": 42,
      "milestone": 9,
      "title": "Fix Supabase client creation in conversation-logs",
      "description": "createClient() is called outside useEffect in ConversationLogs component, creating a new client on every render. Move inside useEffect or memoize.",
      "checks": [
        "Supabase client created inside useEffect only",
        "No unnecessary re-creations on render",
        "Typecheck passes"
      ],
      "passes": true
    },
    {
      "id": 43,
      "milestone": 9,
      "title": "Add redirect for authenticated users on home page",
      "description": "Logged-in users visiting / see the landing page instead of being redirected to dashboard. Add server-side redirect check.",
      "checks": [
        "Authenticated users redirect to /dashboard from /",
        "Unauthenticated users see landing page",
        "Typecheck passes"
      ],
      "passes": true
    }
  ],
  "milestones": {
    "2": "Admin Authentication & Multi-Tenant Structure",
    "3": "Shared Staff Access",
    "4": "Document Upload & Knowledge Base",
    "5": "Staff Chat Assistant",
    "6": "Admin Dashboards & Usage Logs",
    "7": "Testing, Deployment & Handover",
    "8": "Security Hardening",
    "9": "Next.js Hardening"
  }
}

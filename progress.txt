# Ralph Progress — AI Staff Assistant MVP
# Format: [date] story-N: description | outcome | learnings

[2026-06-27] story-1: Supabase project setup | ✅ typecheck passes, tests fail (ECONNREFUSED — need dev server), migration SQL fixed (parenthesized OR in validate_access_token), supabase init created config.toml with email/password auth, .env.local created from example | Fixed SQL operator precedence bug in 004_business_access_tokens.sql — AND/OR nesting without parens. Tests are integration-style (fetch localhost:3000) and cannot pass without dev server — this is expected for infra setup stories.
[2026-06-27] story-2: Tenants table migration + CRUD API | ✅ typecheck passes, tests fail (no dev server). Added GET/PATCH/DELETE /api/tenants/[id] for single-tenant CRUD. Existing POST /api/tenants and GET /api/tenants already in place. Cross-tenant isolation enforced server-side by checking profile.role and profile.tenant_id. | Client_admin can only GET/PATCH own tenant. Super_admin can do everything including DELETE (soft-deactivate). API routes validate auth and role before any operation.
[2026-06-27] story-3: Users table + auth trigger | ✅ typecheck passes, tests mostly timeout (need supabase + seed data). Migration 003 already had table + trigger. Fixed login page (nested getUser() call, added retry for trigger race). Created POST /api/auth/invite for super_admin to invite client_admin users (uses admin.createUser, assigns tenant_id). Added InviteForm to super admin dashboard. | Login page had a bug: nested `await supabase.auth.getUser()` inside itself. The DB trigger that creates public.users can race with the login redirect — added retry loop. Invite flow uses service_role to create auth user, trigger creates public.users row, then PATCH to set tenant_id.
[2026-06-27] story-4: Super Admin login page | ✅ typecheck passes, integration tests fail (no seed data / supabase local stack). Added forgot-password page, reset-password page, auth/callback route, and "Forgot password?" link on login. Login page already handled email/password + role-based redirect + error display. | Password reset uses Supabase PKCE flow: `resetPasswordForEmail` sends email with code, `/auth/callback` exchanges code for session, `/reset-password` form calls `updateUser({ password })`.

